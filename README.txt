1) Place the files in a folder (temple-website).
2) Put your images into the images/ folder and name them as referenced or update src paths.
3) Open index.html in browser or run Live Server from VS Code for convenience.
4) Booking and contact forms are demo-only — stored locally via localStorage. Replace with server endpoints when ready.
5) To convert to React (Month 3 per the PDF): split pages into components, add routing, and move state to hooks/context.
